# Python
__pycache__/
*.pyc
.env
venv/
.venv/
.pytest_cache/

# OS
.DS_Store

# Notebooks
notebooks/.ipynb_checkpoints/

# Local-only outputs (never in git)
artifacts/

# Raw Riot match dumps (too large; keep directory structure via .gitkeep).
# data/raw/compositions_s16.csv IS committed (small, needed for analytics + training).
data/raw/*.json
data/raw/matches.csv
data/raw/matches.csv.gz
data/raw/timelines/

# Crawler checkpoints
data/raw/.crawler_checkpoint.json

# Note: wide_deep.* and *_calibrator.pkl ARE committed (~150 KB total) so a fresh
# clone has all three prob-source backends working out of the box. Don't add
# blanket *.pkl / *.csv ignores back — they previously masked artifacts that the
# demo needs and silently broke the W&D toggle.
